---
interface Props {
  variant?: 'default' | 'home';
  rightLine?: string;
  leftLine?: string;
  screenLabel?: string;
  lastUpdate?: string;   // formatted date string, home variant only
  subtitle?: string;     // sub-masthead strip, home variant only
}
const {
  variant = 'default',
  rightLine,
  leftLine,
  screenLabel,
  lastUpdate,
  subtitle = 'A notebook, sometimes.',
} = Astro.props;
const isHome = variant === 'home';
---
{isHome ? (
  <header
    class="masthead"
    style="border-bottom-width: 4px; border-bottom-style: double; padding-bottom: 18px;"
    data-screen-label={screenLabel}
  >
    <div class="left">Waterloo</div>
    <div class="nameplate fade-in"><em>Postcards</em> from Far Away</div>
    <div class="right">{lastUpdate ?? ''}</div>
  </header>
) : (
  <header class="masthead" data-screen-label={screenLabel}>
    <div class="left">{leftLine ?? 'Praise Prince'}</div>
    <a class="nameplate" href="/"><em>Postcards</em> from Far Away</a>
    <div class="right">{rightLine ?? ''}</div>
  </header>
)}

{isHome && (
  <div class="masthead-sub">
    <span>{subtitle}</span>
  </div>
)}

<style>
  .masthead-sub {
    border-bottom: 1px solid var(--ink);
    padding: 8px clamp(20px, 5vw, 56px);
    text-align: center;
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ink-soft);
  }
</style>
